import 'package:flutter/material.dart';

class DiscipleshipPage extends StatelessWidget {
  const DiscipleshipPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Discipleship Page"));
  }
}